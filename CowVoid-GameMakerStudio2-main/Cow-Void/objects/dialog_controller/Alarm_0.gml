// Detern música menu
audio_stop_sound(snd_musica_menu);

// Reproducir música diálogo
// if (!audio_is_playing(snd_musica_dialog)) {
//     audio_play_sound(snd_musica_dialog, 1, true); // 1 = prioridad, true = loop
// }